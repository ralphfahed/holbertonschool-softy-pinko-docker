# Step 1: Use the latest Ubuntu image
FROM ubuntu:latest

# Step 2 & 3: Update package list and upgrade installed packages
RUN apt-get update && apt-get upgrade -y

# Step 4: Run this command when the container starts
CMD ["echo", "Hello, World!"]
